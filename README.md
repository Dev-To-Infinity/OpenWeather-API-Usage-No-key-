# OpenWeather-API-Usage-No-key-
City search bar (visual implementation) -> weather API call (no visual implementation but it works);
Key must be written into script (Placeholder "!KEY!" in API calls) to work properly
